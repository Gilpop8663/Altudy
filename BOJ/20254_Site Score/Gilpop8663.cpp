#include <bits/stdc++.h>
using namespace std;

int a, b, c, d;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> a >> b >> c >> d;

    int ans = 56 * a + 24 * b + 14 * c + 6 * d;

    cout << ans;
}
