#include <bits/stdc++.h>
using namespace std;

long long n, m;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m;

    if (n == m)
    {
        cout << 1;
    }
    else
    {
        cout << 0;
    }
}
